// Package domain defines the core data types for the kanbin application.
// These types represent the canonical business entities shared across all layers.
// Domain types have no external dependencies — they are plain Go structs.
package domain

// Board types will be defined here in the MVP1 backend phase.
